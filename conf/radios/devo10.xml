<?xml version="1.0"?>
<!--
-- Attributes of root (Radio) tag :
-- name: name of RC transmitter, e.g. Graupner MX22
-- data_min: min width of a pulse to be considered as a data pulse
-- data_max: max width of a pulse to be considered as a data pulse
-- sync_min: min width of a pulse to be considered as a synchro pulse
-- sync_max: max width of a pulse to be considered as a synchro pulse
-- min, max and sync are expressed in micro-seconds
-->

<!--
-- Attributes of channel tag :
-- ctl: name of the command on the transmitter - only for displaying
-- no: order in the PPM frame
-- function: logical command
-- averaged: channel filtered through several frames (for discrete commands)
-- min: minimum pulse length (micro-seconds)
-- max: maximum pulse length (micro-seconds)
-- neutral: neutral pulse length (micro-seconds)
-- Note: a command may be reversed by exchanging min and max values
-->
<!--
The order of the list below is of importance if you do not define a
"no=" (order in the PPM frame) parameter.
If you do not define this then the order of the PPM is the one of
the order of the functon in the list
-->

<!DOCTYPE radio SYSTEM "radio.dtd">
<radio name="Walkera Devo 10" data_min="900" data_max="2100" sync_min="5000" sync_max="15000" pulse_type="POSITIVE">
  <channel ctl="LeftStickVertical"    function="THROTTLE"      min="1090" neutral="1473" max="1862" average="0"/>
  <channel ctl="RightStickHorizontal" function="ROLL"          min="1089" neutral="1473" max="1859" average="0"/>
  <channel ctl="RightStickVertical"   function="PITCH"         min="1090" neutral="1473" max="1863" average="0"/>
  <channel ctl="LeftStickHorizontal"  function="YAW"           min="1986" neutral="1473" max="1050" average="0"/>
  <channel ctl="FMODE"                function="MODE"          min="1000" neutral="1500" max="2000" average="1"/>
  <channel ctl="RuddDR"               function="AUX1"          min="1000" neutral="1000" max="2000" average="1"/>
  <channel ctl="MIX"            	  function="AUX2"          min="1000" neutral="1500" max="1900" average="0"/>
</radio>

