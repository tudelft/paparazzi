<!DOCTYPE autopilot SYSTEM "autopilot.dtd">

<autopilot name="Oneloop Autopilot Cyclone">
  <state_machine name="ap" freq="PERIODIC_FREQUENCY" gcs_mode="true" settings_mode="true" settings_handler="autopilot_generated|SetModeHandler">

    <includes>
      <include name="generated/airframe.h"/>
      <include name="autopilot.h"/>
      <include name="autopilot_rc_helpers.h"/>
      <include name="oneloop/oneloop_andi.h"/>
      <include name="stabilization/stabilization_attitude.h"/>
      <include name="modules/radio_control/radio_control.h"/>
      <include name="modules/actuators/actuators.h"/>
      <include name="modules/actuators/actuators_t4.h"/><!-- should not be needed here fixme -->
      <include name="modules/actuators/actuators_t4_uart.h"/>
      <define  name="RCLost()" value="(radio_control.status == RC_REALLY_LOST)"/>
    </includes>

    <settings>
      <dl_setting var="autopilot.kill_throttle" min="0" step="1" max="1" values="Resurrect|Kill"/> <!-- handler="KillThrottle" -->
    </settings>

    <control_block name="update_v_sp_gcs">
      <call fun="guidance_v_set_z(-nav.nav_altitude)"/>
    </control_block>

    <!-- KILL: 0 -->
    <mode name="KILL">
      <select cond="kill_switch_is_on()"/>
      <on_enter>
        <call fun="autopilot_set_in_flight(false)"/>
        <call fun="autopilot_set_motors_on(false)"/>
      </on_enter>
      <control>
        <call fun="SetCommands(commands_failsafe)"/>
      </control>
    </mode>

    <!-- FAILSAFE: 1 -->
    <mode name="FAILSAFE" shortname="FAIL">    
      <!-- Failsafe does not work needs to be fixed-->
    </mode>

    <!-- HOME: 2 (UNUSED) -->
    <mode name="HOME">
      <exception cond="GpsIsLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- RATE_DIRECT: 3 -->
    <mode name="RATE_DIRECT" shortname="RATE">
      <select cond="RCMode0()"/>
      <on_enter>
        <call fun="oneloop_andi_enter(CONTROL_MODE_RATE, CONTROL_TYPE_ANDI)"/>
      </on_enter>
      <control>
        <call fun="oneloop_andi_run(CONTROL_MODE_RATE)"/>
      </control>
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- ATITUDE_DIRECT: 4 -->
    <mode name="ATTITUDE_DIRECT" shortname="ATT">
      <select cond="RCMode1()"/>
      <select cond="$DEFAULT_MODE"/>
      <on_enter>
        <call fun="oneloop_andi_enter(CONTROL_MODE_ATTITUDE, CONTROL_TYPE_ANDI)"/>
      </on_enter>
      <control>
        <call fun="oneloop_andi_run(CONTROL_MODE_ATTITUDE)"/>
      </control>
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- RATE_RC_CLIMB: 5 (UNUSED) -->
    <mode name="RATE_RC_CLIMB" shortname="RRCC">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- ATTITUDE_RC_CLIMB: 6 (UNUSED) -->
    <mode name="ATTITUDE_RC_CLIMB" shortname="ATTITUDE_RC_CLIMB">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- ATTITUDE_CLIMB: 7 (UNUSED) -->
    <mode name="ATTITUDE_CLIMB" shortname="ATTITUDE_CLIMB">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- RATE_Z_HOLD: 8 (UNUSED) -->
    <mode name="RATE_Z_HOLD" shortname="RATE_Z_HOLD">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- ATTITUDE_Z_HOLD: (UNUSED) 9 -->
    <mode name="ATTITUDE_Z_HOLD" shortname="A_ZH">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- HOVER_DIRECT: 10 (UNUSED) -->
    <mode name="HOVER_DIRECT" shortname="HOVER_DIRECT">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- HOVER_CLIMB: 11 (UNUSED) -->
    <mode name="HOVER_CLIMB" shortname="HOVER_CLIMB">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- HOVER_Z_HOLD: 12 (UNUSED) -->
    <mode name="HOVER_Z_HOLD" shortname="HOVER_Z_HOLD">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- NAV: 13 -->
    <mode name="NAV" shortname="NAV">
      <select cond="RCMode2()" exception="HOME"/>
      <on_enter>
        <call fun="oneloop_andi_enter(CONTROL_MODE_GUIDANCE, CONTROL_TYPE_ANDI)"/>
      </on_enter>
      <!-- <control>
        <call fun="guidance_h_mode_changed(GUIDANCE_H_MODE_NAV)"/>
        <call fun="guidance_v_mode_changed(GUIDANCE_V_MODE_NAV)"/>
        <call fun="guidance_h_set_pos(nav.carrot.y, nav.carrot.x)"/>
        <call fun="guidance_v_set_z(-nav.nav_altitude)"/>
        <call fun="oneloop_from_nav(autopilot_in_flight())"/>
      </control> -->
      <control>
        <call fun="oneloop_andi_run(CONTROL_MODE_GUIDANCE)"/>
      </control>
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- RC_DIRECT: 14 (UNUSED) -->
    <mode name="RC_DIRECT" shortname="RC_DIRECT">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- CARE_FREE_DIRECT: 15 (UNUSED) -->
    <mode name="CARE_FREE_DIRECT" shortname="CARE_FREE_DIRECT">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>
    
    <!-- FORWARD: 16 (UNUSED) -->
    <mode name="FORWARD" shortname="FORWARD">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- MODULE: 17 (UNUSED) -->
    <mode name="MODULE" shortname="MODULE">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- FLIP: 18 (UNUSED) -->
    <mode name="FLIP" shortname="FLIP">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>

    <!-- GUIDED: 19 (UNUSED) -->
    <mode name="GUIDED">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>  

    <!-- ATTITUDE_DIRECT_FWD: 20 (UNUSED) -->
    <mode name="ATTITUDE_DIRECT_FWD" shortname="ATT_Fwd">
      <exception cond="RCLost()" deroute="FAILSAFE"/>
    </mode>
  
  </state_machine>
</autopilot>
